var React = require('react'),

    Pass = React.createClass({
        render: function() {
            return (
                <form>
                    <h1>Pass</h1>

                    <label>Verified:</label>
                    <textarea></textarea>
                </form>
                );
        }
});

module.exports = Pass;
